# 腾讯元器嵌入指南 - 免费接入方式

## 🎯 方案对比

| 方案 | 费用 | 难度 | 推荐度 |
|------|------|------|--------|
| **iframe嵌入** | 免费 | 简单 | ⭐⭐⭐⭐⭐ |
| Webhook回调 | 免费 | 中等 | ⭐⭐⭐ |
| 云函数代理 | 按量付费 | 中等 | ⭐⭐ |

## 📱 iframe嵌入方式（推荐）

### 步骤1：获取嵌入链接

1. 登录 [腾讯元器控制台](https://yuanqi.tencent.com)
2. 进入您的"老张工艺助手"智能体
3. 点击 **「发布」** 按钮
4. 选择 **「网页嵌入」** 或 **「分享」**
5. 复制嵌入链接

嵌入链接格式示例：
```
https://yuanqi.tencent.com/chat/xxxxx
```

### 步骤2：修改前端配置

打开 `js/chat.js`，修改以下配置：

```javascript
config: {
  mode: 'iframe',  // 改为 'iframe' 启用嵌入模式
  
  iframe: {
    // 粘贴从腾讯元器复制的链接
    url: 'https://yuanqi.tencent.com/chat/xxxxx',
    containerId: 'yuanqi-iframe-container'
  }
}
```

### 步骤3：刷新页面

保存后刷新网页，对话窗口将显示腾讯元器的官方聊天界面。

---

## 🔗 Webhook回调方式

### 适用场景
- 需要自定义UI界面
- 需要保存对话记录到本地
- 需要与其他系统集成

### 实现步骤

#### 1. 配置Webhook地址

1. 在腾讯元器控制台 → 智能体设置 → Webhook
2. 填写您的服务器地址：
   ```
   https://your-domain.com/webhook
   ```
3. 设置触发事件（如：收到消息时）

#### 2. 搭建Webhook接收服务

由于Webhook需要公网服务器，推荐使用以下**免费方案**：

**方案A：腾讯云开发CloudBase（有免费额度）**
```javascript
// 云函数示例
const cloud = require('wx-server-sdk');
cloud.init();

exports.main = async (event, context) => {
  const { message, user_id } = event;
  
  // 调用腾讯元器API
  const response = await fetch('https://yuanqi.tencent.com/api/v1/chat', {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer oKmFAEybU7IkzqWmNbdTdyv4KMLifhYf'
    },
    body: JSON.stringify({
      assistant_id: 'oKmFAEybU7IkzqWmNbdTdyv4KMLifhYf',
      message: message
    })
  });
  
  return await response.json();
};
```

**方案B：Vercel Serverless Functions（免费）**
- 部署简单
- 有免费额度
- 全球CDN加速

**方案C：GitHub Codespaces / Gitpod**
- 开发测试使用
- 临时公网地址

---

## 💡 比赛演示建议

### 推荐方案：模拟回复 + iframe备选

当前代码已内置**模拟回复模式**，特点：
- ✅ 完全免费
- ✅ 无需服务器
- ✅ 加载速度快
- ✅ 已覆盖常见工艺问题

**演示流程：**
1. 比赛演示时使用模拟回复模式（`mode: 'mock'`）
2. 答辩时展示iframe嵌入方式已配置好
3. 说明实际部署时可切换为真实API

### 常见问题预设回复

当前模拟模式已支持：
| 关键词 | 回复内容 |
|--------|----------|
| 车削/车床 | 车削原理、参数、注意事项 |
| 铣削/铣床 | 铣削特点、铣刀类型、安全提醒 |
| 钻/孔 | 钻孔要点、麻花钻使用技巧 |
| 淬火/热处理 | 热处理工艺、淬火回火区别 |
| 45钢 | 材料特性、加工工艺路线 |
| 磨削/砂轮 | 磨削精度、砂轮选择、安全操作 |
| 铸造/锻造 | 毛坯制造方法对比 |

---

## 🚀 快速部署到公网

### 方案1：GitHub Pages（完全免费）

1. 创建GitHub仓库
2. 上传项目代码
3. 开启GitHub Pages功能
4. 获得 `https://username.github.io/repo` 地址

### 方案2：Vercel（完全免费）

1. 注册 [Vercel](https://vercel.com) 账号
2. 导入GitHub仓库
3. 自动部署，获得 `.vercel.app` 域名

### 方案3：腾讯云COS（有免费额度）

1. 创建COS存储桶
2. 开启静态网站托管
3. 上传文件
4. 获得访问地址

---

## 📋 配置文件参考

### 纯模拟模式（默认，免费）
```javascript
config: {
  mode: 'mock',
  iframe: {
    url: '',
    containerId: 'yuanqi-iframe-container'
  }
}
```

### iframe嵌入模式（免费）
```javascript
config: {
  mode: 'iframe',
  iframe: {
    url: 'https://yuanqi.tencent.com/chat/xxxxx',
    containerId: 'yuanqi-iframe-container'
  }
}
```

---

## ❓ 常见问题

### Q1: iframe嵌入有广告吗？
**A:** 腾讯元器的嵌入界面通常带有品牌标识，但不影响使用。

### Q2: 免费额度够用吗？
**A:** 
- GitHub Pages：完全免费，无流量限制
- Vercel：每月100GB流量，完全够用
- 腾讯元器API：查看官方免费额度说明

### Q3: 如何测试API是否正常？
**A:** 在浏览器控制台执行：
```javascript
fetch('https://yuanqi.tencent.com/api/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer oKmFAEybU7IkzqWmNbdTdyv4KMLifhYf'
  },
  body: JSON.stringify({
    assistant_id: 'oKmFAEybU7IkzqWmNbdTdyv4KMLifhYf',
    message: '你好'
  })
}).then(r => r.json()).then(console.log)
```

---

## 📞 技术支持

- 腾讯元器文档：https://yuanqi.tencent.com/docs
- 腾讯元器社区：https://yuanqi.tencent.com/community
